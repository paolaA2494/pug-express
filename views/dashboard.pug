extends header
block content
    .content-form
        h1 Bienvenido!
        if datos
            h2 #{datos.email}
            h2 Lista de Alumnos
            ul
                each nombre in datos.alumnos
                    li= nombre
            
        else
            h2 datos no disponibles     